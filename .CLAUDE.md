# CLAUDE.md — VCA-Diffusion

## 이 파일을 읽는 Claude Code에게

규칙은 단 두 개다.

1. **순서를 지켜라**: Phase 0 → 1 → 2 → 3 → 4. 이전 Phase의 pytest가 전부 PASSED여야 다음으로 넘어간다.
2. **파일을 먼저 만들어라**: 구현 코드보다 `conftest.py`와 `pytest.ini`를 먼저 만든다.

모든 실행은 이 한 줄이다:
```bash
pytest -v --tb=short -p no:randomly          # 전체
pytest -v -m phase1                          # 특정 Phase만
pytest -v --lf                               # 실패한 것만 재실행
```

---

---

## 핵심 수식 — 구현 전 반드시 읽어라

### 왜 Sigmoid인가 (Softmax 절대 금지)

일반 cross-attention은 Softmax를 쓴다:
```
w[n] = exp(Q·K[n]) / sum_n exp(Q·K[n])   # zero-sum: 한 entity가 올라가면 다른 entity는 강제로 내려감
```

이러면 두 entity가 겹치는 픽셀에서 "한 entity의 attention이 높아지면 다른 entity는 0에 수렴"한다.
모델은 한 entity를 아예 렌더링하지 않는 방향으로 수렴 → **Disappearance(객체 소실)**.

VCA는 Sigmoid를 쓴다:
```
sigma[s, n, z] = sigmoid(Q[s] · K[n,z] / sqrt(D))   # 각 entity가 독립적으로 [0,1] 밀도를 가짐
```

sigma는 "이 픽셀에서 이 entity가 이 깊이에 존재할 확률"이다.
entity 간 경쟁이 없으므로 두 entity가 동시에 높은 밀도를 가질 수 있다.

### Ray-Marching Transmittance

NeRF의 volume rendering에서 가져온 개념.
깊이 z에 도달하는 빛의 비율 T[z]를 앞쪽 모든 entity의 밀도로 계산한다:

```
opacity[s, z]  = clamp(sum_n sigma[s, n, z], max=1.0)
T[s, z=0]      = 1.0                                        # 아무것도 가리지 않음
T[s, z=k]      = prod_{z'=0}^{k-1} (1 - opacity[s, z'])    # 앞 슬라이스들이 빛을 막음
weight[s, n, z] = T[s, z] * sigma[s, n, z]                 # 최종 attention weight
output[s]       = sum_{n,z} weight[s, n, z] * V[n, z]
```

**핵심 직관**: 앞 entity(z=0)의 sigma가 크면 T[z=1]이 작아진다.
뒤 entity(z=1)의 weight = T[z=1] * sigma[z=1]도 자동으로 작아진다.
외부 마스크 없이도 앞 entity가 뒤 entity를 물리적으로 가린다.

### cumprod 구현 시 off-by-one 주의

```python
# WRONG: T[z=0]이 1.0이 아님
T = torch.cumprod(1.0 - opacity, dim=-1)

# CORRECT: exclusive cumprod → T[z=0]=1, T[z=1]=1-opacity[0], T[z=2]=(1-opacity[0])*(1-opacity[1])
ones = torch.ones(*opacity.shape[:2], 1, device=opacity.device)
T = torch.cumprod(torch.cat([ones, 1.0 - opacity], dim=-1), dim=-1)[:, :, :-1]
```

---

## 프로젝트 구조

```
vca_diffusion/
├── CLAUDE.md
├── conftest.py
├── pytest.ini
├── requirements.txt
│
├── harness/
│   ├── test_phase0_env.py
│   ├── test_phase1_vca.py
│   ├── test_phase2_toy.py
│   ├── test_phase3_papers.py
│   └── test_phase4_failures.py
│
├── models/
│   └── vca_attention.py
├── toy/
│   └── generate_toy_data.py
└── docs/
    ├── related_papers.md
    └── failures.md
```

---

## 설정 파일 (가장 먼저 만들 것)

### `requirements.txt`
```
torch>=2.1.0
diffusers>=0.27.0
transformers>=4.38.0
einops>=0.7.0
pyvista[all]>=0.43.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9
matplotlib>=3.8.0
numpy>=1.26.0
tqdm>=4.66.0
pytest>=8.0.0
pytest-order>=1.2.0
```

### `pytest.ini`
```ini
[pytest]
testpaths = harness
markers =
    phase0: Environment
    phase1: VCA unit tests
    phase2: Toy data generation
    phase3: Paper documentation
    phase4: Failure documentation
addopts = --tb=short -v
```

### `conftest.py`
```python
import pytest

_phase_failed: set[int] = set()

def pytest_runtest_makereport(item, call):
    if call.when == "call" and call.excinfo is not None:
        phase = _get_phase(item)
        if phase is not None:
            _phase_failed.add(phase)

def pytest_runtest_setup(item):
    phase = _get_phase(item)
    if phase is None or phase == 0:
        return
    for prev in range(phase):
        if prev in _phase_failed:
            pytest.skip(f"Phase {prev} failed — skipping Phase {phase}")

def _get_phase(item):
    for m in item.iter_markers():
        if m.name.startswith("phase"):
            try:
                return int(m.name.replace("phase", ""))
            except ValueError:
                pass
    return None
```

---

## Phase 0 — `harness/test_phase0_env.py`

```python
import importlib
import pytest

PACKAGES = ["torch", "diffusers", "transformers", "einops", "pyvista", "imageio", "numpy"]

@pytest.mark.phase0
@pytest.mark.parametrize("pkg", PACKAGES)
def test_import(pkg):
    assert importlib.import_module(pkg) is not None

@pytest.mark.phase0
def test_torch_version():
    import torch
    assert int(torch.__version__.split(".")[0]) >= 2

@pytest.mark.phase0
def test_pyvista_offscreen():
    import pyvista as pv
    pv.start_xvfb()          # headless 환경 자동 처리
    pl = pv.Plotter(off_screen=True)
    pl.add_mesh(pv.Sphere())
    pl.render()
    img = pl.screenshot(return_img=True)
    pl.close()
    assert img.shape[2] == 3  # RGB
```

---

## Phase 1 — `models/vca_attention.py` 구현 후 `harness/test_phase1_vca.py`

### 구현 (`models/vca_attention.py`)

**텐서 규약 — 절대 바꾸지 마라**
```
BF = B * F   (batch × frames)
S  = H * W   (spatial tokens)
D  = query_dim
N  = n_entities  (Phase 1: 2 고정)
Z  = z_bins      (Phase 1: 2 고정)
CD = context_dim (CLIP: 768)
```

```python
import torch
import torch.nn as nn
import torch.nn.functional as F
from einops import rearrange
from typing import Optional


class LoRALinear(nn.Module):
    """base weight frozen, LoRA branch만 학습.
    초기화: lora_B=zeros → 초기 LoRA 기여 0 (원 논문 방식, 분산 안정)
    lora_A=randn*0.01은 의도적. kaiming으로 바꾸지 마라 — lora_B=zeros라 초기 출력은 어차피 0.
    """
    def __init__(self, in_f: int, out_f: int, rank: int = 8):
        super().__init__()
        self.weight = nn.Parameter(torch.empty(out_f, in_f), requires_grad=False)
        nn.init.kaiming_uniform_(self.weight)
        self.lora_A = nn.Parameter(torch.randn(rank, in_f) * 0.01)
        self.lora_B = nn.Parameter(torch.zeros(out_f, rank))

    def forward(self, x):
        return F.linear(x, self.weight) + F.linear(F.linear(x, self.lora_A), self.lora_B)


class VCALayer(nn.Module):
    """
    Volumetric Cross-Attention.
    핵심: Sigmoid (독립 밀도) + Transmittance (물리적 occlusion)
    절대로 Softmax로 바꾸지 마라 — zero-sum이 되어 Disappearance 발생

    context_dim 선택:
      SD 1.5  기반 AnimateDiff → context_dim=768
      SDXL    기반 (Dual CLIP) → context_dim=2048
    파이프라인을 확정하기 전까지 하드코딩하지 말고 반드시 인자로 받아라.
    """
    def __init__(self, query_dim, context_dim: int, n_heads=8,
                 n_entities=2, z_bins=2, lora_rank=8):
        # context_dim에 기본값 없음 — 호출부에서 명시적으로 지정 강제
        super().__init__()
        assert query_dim % n_heads == 0
        self.n_heads, self.n_entities, self.z_bins = n_heads, n_entities, z_bins
        self.head_dim = query_dim // n_heads
        self.scale = self.head_dim ** -0.5

        self.to_q = nn.Linear(query_dim, query_dim, bias=False)
        self.to_k = LoRALinear(context_dim, query_dim, rank=lora_rank)
        self.to_v = LoRALinear(context_dim, query_dim, rank=lora_rank)
        self.depth_pe = nn.Parameter(torch.randn(z_bins, context_dim) * 0.02)
        self.to_out = nn.Linear(query_dim, query_dim)

        self.last_sigma: Optional[torch.Tensor] = None         # (BF, S, N, Z)
        self.last_transmittance: Optional[torch.Tensor] = None # (BF, S, Z)

    def _expand_context(self, ctx):
        # ctx: (BF, N, CD) → (BF, N*Z, CD)
        pe = self.depth_pe.unsqueeze(0).unsqueeze(0)           # (1,1,Z,CD)
        ctx3d = ctx.unsqueeze(2) + pe                          # (BF,N,Z,CD)
        return rearrange(ctx3d, 'bf n z cd -> bf (n z) cd')

    def _transmittance(self, sigma):
        # sigma: (BF,S,N,Z) → T: (BF,S,Z)
        # T[z=0]=1, T[z=k]=prod_{z'<k}(1 - clamp(sum_n sigma[z'], max=1))
        opacity = sigma.sum(dim=2).clamp(max=1.0)
        ones = torch.ones(*opacity.shape[:2], 1, device=sigma.device, dtype=sigma.dtype)
        return torch.cumprod(torch.cat([ones, 1.0 - opacity], dim=-1), dim=-1)[:, :, :-1]

    def forward(self, x, context):
        # x: (BF,S,D)  context: (BF,N,CD)
        BF, S, _ = x.shape
        N, Z, h = self.n_entities, self.z_bins, self.n_heads

        Q = rearrange(self.to_q(x), 'bf s (h d) -> bf h s d', h=h)
        ctx3d = self._expand_context(context)
        K = rearrange(self.to_k(ctx3d), 'bf (n z) (h d) -> bf h (n z) d', h=h, n=N, z=Z)
        V = rearrange(self.to_v(ctx3d), 'bf (n z) (h d) -> bf h (n z) d', h=h, n=N, z=Z)

        scores = torch.einsum('bhsd,bhtd->bhst', Q, K) * self.scale
        sigma  = torch.sigmoid(rearrange(scores, 'bf h s (n z) -> bf h s n z', n=N, z=Z))

        sigma_mean = sigma.mean(dim=1)                         # (BF,S,N,Z)
        T = self._transmittance(sigma_mean)                    # (BF,S,Z)

        weights = T.unsqueeze(1).unsqueeze(3) * sigma          # (BF,h,S,N,Z)
        weights = rearrange(weights, 'bf h s n z -> bf h s (n z)')
        out = rearrange(torch.einsum('bhst,bhtd->bhsd', weights, V), 'bf h s d -> bf s (h d)')

        self.last_sigma = sigma_mean.detach()
        self.last_transmittance = T.detach()

        return x + self.to_out(out)
```

### 테스트 (`harness/test_phase1_vca.py`)

```python
import pytest
import torch

pytestmark = pytest.mark.phase1

@pytest.fixture(scope="module")
def layer():
    from models.vca_attention import VCALayer
    # context_dim=768: SD 1.5 기준 단위 테스트용 (SDXL이면 2048로 바꿔라)
    return VCALayer(query_dim=320, context_dim=768, n_heads=8, n_entities=2, z_bins=2)

@pytest.fixture(scope="module")
def fwd(layer):
    x   = torch.randn(4, 1024, 320)
    ctx = torch.randn(4, 2, 768)
    out = layer(x, ctx)
    return x, ctx, out, layer

# Shape
def test_output_shape(fwd):
    x, _, out, _ = fwd
    assert out.shape == x.shape

def test_sigma_shape(fwd):
    assert fwd[3].last_sigma.shape == (4, 1024, 2, 2)

def test_transmittance_shape(fwd):
    assert fwd[3].last_transmittance.shape == (4, 1024, 2)

# Physics
def test_sigma_range(fwd):
    s = fwd[3].last_sigma
    assert s.min() >= 0.0 and s.max() <= 1.0

def test_T_z0_is_one(fwd):
    T = fwd[3].last_transmittance
    assert torch.allclose(T[:, :, 0], torch.ones_like(T[:, :, 0]))

def test_T_monotone(fwd):
    T = fwd[3].last_transmittance
    assert (T[:, :, 0] >= T[:, :, 1] - 1e-5).all()

# Gradient
def test_grad_flows_to_lora():
    from models.vca_attention import VCALayer
    l = VCALayer(query_dim=320, context_dim=768, n_heads=8, n_entities=2, z_bins=2)
    l(torch.randn(2, 64, 320), torch.randn(2, 2, 768)).mean().backward()
    assert l.to_k.lora_A.grad is not None
    assert l.depth_pe.grad    is not None

def test_base_weight_frozen():
    from models.vca_attention import VCALayer
    l = VCALayer(query_dim=320, context_dim=768, n_heads=8, n_entities=2, z_bins=2)
    l(torch.randn(2, 64, 320), torch.randn(2, 2, 768)).mean().backward()
    assert l.to_k.weight.grad is None

# Sigmoid vs Softmax 검증
def test_entities_independent():
    """
    FM-I4 반영: ctx = [+5, -5] 설정은 K anti-symmetric을 만들어
    sigmoid(x)+sigmoid(-x)=1 항등식으로 diff ≈ 0이 된다 — 잘못된 테스트.

    올바른 방법: ctx = [+5, +5] (동일 방향)
    → σ_entity0[s] = σ_entity1[s] (같은 점수)
    → 두 entity가 동시에 high인 비율 ≈ 50% (Sigmoid)
    → Softmax라면 zero-sum → both_high ≈ 0%
    """
    from models.vca_attention import VCALayer
    CD = 768
    l = VCALayer(query_dim=320, context_dim=CD, n_heads=8, n_entities=2, z_bins=2)
    ctx = torch.zeros(2, 2, CD)
    ctx[:, 0, :] = 5.0   # entity 0: large positive
    ctx[:, 1, :] = 5.0   # entity 1: same direction — 반대(±5)가 아님!
    l(torch.randn(2, 64, 320), ctx)
    s = l.last_sigma  # (BF, S, N, Z)
    both_high = ((s[:,:,0,:] > 0.5) & (s[:,:,1,:] > 0.5)).float().mean()
    assert both_high.item() > 0.1, \
        f"Sigmoid should allow both entities high simultaneously. " \
        f"both_high={both_high:.4f} (Softmax would give ~0)"
```

---

## Phase 2 — `toy/generate_toy_data.py` 구현 후 `harness/test_phase2_toy.py`

### 구현 (`toy/generate_toy_data.py`)

두 시나리오를 모두 구현한다.

**시나리오 A — Chain Links**
- ㄷ자 tube 두 개 (반원 arc + 직선 cylinder 조합)
- Link 0: XZ 평면, 빨강 / Link 1: YZ 평면, 파랑
- 각 link가 자신의 축으로 천천히 회전 → 프레임 7~9에서 교차 최대
- PyVista: `pv.Spline().tube()` + `pv.Line().tube()`

**시나리오 B — Robot Arms**
- 3-joint arm 두 개 (Cylinder + Sphere 조합)
- Arm 0: (-0.4,0,0) 기준 빨강 / Arm 1: (+0.4,0,0) 기준 파랑
- Forward kinematics로 shoulder/elbow 각도 시간에 따라 변화
- 두 forearm이 중앙에서 점점 더 깊게 교차

**Robot Arm Forward Kinematics — 좌표 계산 (이대로 구현하라)**

각 joint의 world position을 순서대로 계산한다.
회전행렬 R_y(θ)를 누적 적용하는 방식:

```python
def rot_y(deg):
    r = np.radians(deg)
    return np.array([[np.cos(r), 0, np.sin(r)],
                     [0,         1, 0        ],
                     [-np.sin(r),0, np.cos(r)]])

# Arm 0 기준 (base_pos = [-0.4, 0, 0])
# joint 길이: upper_arm=0.6, forearm=0.5

shoulder_world = base_pos + [0, 0, 0.1]          # base 높이
elbow_world    = shoulder_world + R_y(shoulder) @ [0, 0, 0.6]
wrist_world    = elbow_world    + R_y(shoulder + elbow) @ [0, 0, 0.5]

# 각 파트 mesh를 해당 위치로 translate
# upper_arm: shoulder_world → elbow_world 방향으로 cylinder
# forearm:   elbow_world   → wrist_world  방향으로 cylinder
# pv.Cylinder는 center와 direction으로 배치
```

Arm 1은 base_pos=[+0.4, 0, 0], 각도 부호 반전(shoulder=-arm0_shoulder, elbow=-arm0_elbow).
두 forearm이 프레임 7~9에서 중앙(0,0,z)에서 교차하도록 각도를 설정하라.



**공통 출력 구조** (시나리오별):
```
toy/data/{scenario}/{camera_name}/
    frames/   0000.png … 0015.png   (RGB 512×512)
    depth/    0000.npy … 0015.npy   (float32 depth map)
    mask/     0000_entity0.png, 0000_entity1.png … (binary mask)
    video.mp4
```

**카메라 앵글 목록** (index 순서 고정):
```python
CAMERAS = [
    {"position": ( 3.0,  3.0, 2.5), "name": "front_right"},
    {"position": (-3.0,  3.0, 2.5), "name": "front_left"},
    {"position": ( 0.0,  4.0, 1.0), "name": "front"},
    {"position": ( 0.0,  0.5, 4.0), "name": "top"},
    {"position": ( 3.0, -3.0, 2.5), "name": "back_right"},
    {"position": (-3.0, -3.0, 2.5), "name": "back_left"},
    {"position": ( 4.0,  0.0, 0.5), "name": "side_right"},
    {"position": (-4.0,  0.0, 0.5), "name": "side_left"},
]
```

**렌더링 함수 시그니처** (반드시 이 인터페이스를 지켜라):
```python
class SceneRenderer:
    def __init__(self, width=512, height=512): ...

    def render_frame(
        self,
        meshes: list[tuple[pv.PolyData, tuple]],  # [(mesh, rgb_color), ...]
        camera_pos: tuple,
        focal_point=(0, 0, 0),
        view_up=(0, 0, 1),
    ) -> tuple[np.ndarray, np.ndarray, list[np.ndarray]]:
        # 반환: (rgb HxWx3 uint8, depth HxW float32, [mask_e0, mask_e1, ...])
        # mask: entity만 남기고 나머지 숨겨서 배경과 비교해 추출
```

**CLI**:
```bash
python toy/generate_toy_data.py                         # 두 시나리오 모두, 카메라 1개
python toy/generate_toy_data.py --scenario chain
python toy/generate_toy_data.py --scenario robot_arm
python toy/generate_toy_data.py --n-cameras 4           # 멀티앵글
python toy/generate_toy_data.py --n-frames 16 --resolution 512
```

### 테스트 (`harness/test_phase2_toy.py`)

```python
import subprocess, sys, json
from pathlib import Path
import numpy as np
import pytest

pytestmark = pytest.mark.phase2
SCENARIOS = ["chain", "robot_arm"]
CAM = "front_right"

@pytest.fixture(scope="module")
def generated():
    r = subprocess.run(
        [sys.executable, "toy/generate_toy_data.py",
         "--n-frames", "16", "--n-cameras", "1", "--resolution", "256"],
        capture_output=True, text=True, timeout=300
    )
    return r

def test_exits_cleanly(generated):
    assert generated.returncode == 0, generated.stderr[-600:]

@pytest.mark.parametrize("scenario", SCENARIOS)
def test_video_created(generated, scenario):
    p = Path(f"toy/data/{scenario}/{CAM}/video.mp4")
    assert p.exists() and p.stat().st_size > 5_000

@pytest.mark.parametrize("scenario", SCENARIOS)
def test_frame_count(generated, scenario):
    frames = list(Path(f"toy/data/{scenario}/{CAM}/frames").glob("*.png"))
    assert len(frames) == 16

@pytest.mark.parametrize("scenario", SCENARIOS)
def test_depth_dtype(generated, scenario):
    d = np.load(f"toy/data/{scenario}/{CAM}/depth/0007.npy")
    assert d.dtype == np.float32 and d.shape == (256, 256)

@pytest.mark.parametrize("scenario", SCENARIOS)
def test_masks_created(generated, scenario):
    masks = list(Path(f"toy/data/{scenario}/{CAM}/mask").glob("*.png"))
    assert len(masks) == 32  # 16 frames × 2 entities

@pytest.mark.parametrize("scenario", SCENARIOS)
def test_entity_masks_differ(generated, scenario):
    """두 entity mask가 서로 다른 영역을 커버해야 한다 (IoU < 0.8)"""
    import imageio.v3 as iio
    m0 = iio.imread(f"toy/data/{scenario}/{CAM}/mask/0007_entity0.png") > 128
    m1 = iio.imread(f"toy/data/{scenario}/{CAM}/mask/0007_entity1.png") > 128
    iou = (m0 & m1).sum() / max((m0 | m1).sum(), 1)
    assert iou < 0.8, f"Masks overlap too much IoU={iou:.2f}"

@pytest.mark.parametrize("scenario", SCENARIOS)
def test_depth_ordering_in_overlap(generated, scenario):
    """
    핵심 ground truth 검증:
    두 entity가 겹치는 픽셀에서 depth 값이 달라야 한다.
    이 값이 나중에 VCA sigma 검증의 ground truth가 된다.
    """
    import imageio.v3 as iio
    depth = np.load(f"toy/data/{scenario}/{CAM}/depth/0007.npy")
    m0 = iio.imread(f"toy/data/{scenario}/{CAM}/mask/0007_entity0.png") > 128
    m1 = iio.imread(f"toy/data/{scenario}/{CAM}/mask/0007_entity1.png") > 128
    overlap = m0 & m1
    if overlap.sum() < 10:
        pytest.skip("Overlap too small at frame 7 — occlusion not visible from this angle")
    # overlap & m0 == overlap 이므로 의미 없음
    # 대신 각 entity 단독 영역의 평균 depth를 비교
    m0_only = m0 & ~m1
    m1_only = m1 & ~m0
    if m0_only.sum() < 5 or m1_only.sum() < 5:
        pytest.skip("Non-overlapping regions too small — adjust geometry")
    d0 = depth[m0_only].mean()
    d1 = depth[m1_only].mean()
    assert abs(d0 - d1) > 0.01, \
        f"Entity depth planes too similar (d0={d0:.3f}, d1={d1:.3f}) — entities at same Z"

def test_prompts_json(generated):
    data = json.loads(Path("toy/data/prompts.json").read_text())
    for s in SCENARIOS:
        assert "entity_0" in data[s] and "entity_1" in data[s]
```

---

## Phase 3 — `docs/related_papers.md` 작성

코드 없음. 아래 형식으로 10편을 정리한다.
**단순 요약 금지. 각 논문마다 "VCA와의 차이점" 섹션이 반드시 있어야 한다.**

```markdown
## [논문 제목] (저자, 년도, 학회)
- **핵심 아이디어**: 한 문장
- **해결 못한 문제**: (VCA의 동기와 연결)
- **VCA와의 차이점**: 구체적으로
- **참고할 기술 요소**: 코드 레벨
```

필수 10편:
- Attend-and-Excite (Chefer, SIGGRAPH 2023)
- PEEKABOO (Jain, CVPR 2024)
- TokenFlow (Geyer, ICLR 2024)
- CONFORM (Meral, 2024)
- NeRF (Mildenhall, ECCV 2020) ← transmittance 수식 원천
- LoRA (Hu, ICLR 2022)
- AnimateDiff (Guo, ICLR 2024)
- VideoComposer (Wang, NeurIPS 2023)
- MotionDirector (Zhao, 2024)
- FLATTEN (Cong, ICLR 2024)

### 테스트 (`harness/test_phase3_papers.py`)

```python
from pathlib import Path
import pytest

pytestmark = pytest.mark.phase3

PAPERS = [
    ("Attend-and-Excite", "Chefer"), ("PEEKABOO", "Jain"),
    ("TokenFlow", "Geyer"),          ("CONFORM", "Meral"),
    ("NeRF", "Mildenhall"),          ("LoRA", "Hu"),
    ("AnimateDiff", "Guo"),          ("VideoComposer", "Wang"),
    ("MotionDirector", "Zhao"),      ("FLATTEN", "Cong"),
]

@pytest.fixture(scope="module")
def content():
    p = Path("docs/related_papers.md")
    assert p.exists(), "docs/related_papers.md not found"
    return p.read_text(encoding="utf-8")

@pytest.mark.parametrize("paper,author", PAPERS)
def test_paper_present(content, paper, author):
    assert paper in content and author in content

def test_vca_diff_present(content):
    assert "VCA" in content and ("차이" in content or "differs" in content.lower())

def test_min_length(content):
    assert len(content.split()) >= 800, f"Too short: {len(content.split())} words"
```

---

## Phase 4 — `docs/failures.md` 작성

Phase 1~2 진행 중 발생한 실제 오류를 기록한다.
아래 이론적 실패 모드는 반드시 포함해야 한다.

```markdown
## 이론적 실패 모드

### FM-T1: Softmax 사용 시 Disappearance
### FM-T2: Transmittance off-by-one (T[z=0] ≠ 1.0)
### FM-T3: opacity clamp 누락 (T가 음수)
### FM-T4: L_ortho 없을 때 z=0 몰림

## 통합 실패 모드

### FM-A1: diffusers hook 미호출
### FM-A2: float16/float32 dtype 불일치

## 실제 발생한 실패 (FM-I)

### FM-I1: [Phase 1~2 진행 중 실제 발생한 것 기록]
```

### 테스트 (`harness/test_phase4_failures.py`)

```python
from pathlib import Path
import pytest

pytestmark = pytest.mark.phase4

REQUIRED = ["FM-T1","FM-T2","FM-T3","FM-T4","FM-A1","FM-A2","FM-I"]

@pytest.fixture(scope="module")
def content():
    p = Path("docs/failures.md")
    assert p.exists()
    return p.read_text(encoding="utf-8")

@pytest.mark.parametrize("tag", REQUIRED)
def test_section_present(content, tag):
    assert tag in content, f"{tag} section missing"

def test_solutions_present(content):
    for tag in ["FM-T1","FM-T2","FM-T3"]:
        idx = content.find(tag)
        section = content[idx:idx+600]
        has_solution = any(w in section for w in ["해결","fix","solution","수정"])
        assert has_solution, f"{tag} has no solution documented"
```

---

## 작업 순서 요약

```
Step 1: conftest.py, pytest.ini, requirements.txt 생성
Step 2: pip install -r requirements.txt
        pytest -m phase0  → PASSED 확인

Step 3: models/__init__.py, models/vca_attention.py 생성
        pytest -m phase1  → 실패 시 --lf로 반복 수정

Step 4: toy/generate_toy_data.py 생성
        pytest -m phase2  → 실패 시 --lf로 반복 수정

Step 5: docs/related_papers.md 작성
        pytest -m phase3

Step 6: docs/failures.md 작성 (Phase 1~2 실패 기록 포함)
        pytest -m phase4

Step 7: models/vca_processor.py, scripts/debug_gif.py 구현
        pytest -m phase5

Step 8: models/losses.py, scripts/train_vca.py 구현
        pytest -m phase6

Step 9: pytest -v  (전체 최종 확인)
```

---

## Phase 5 — Inference 주입 + GIF 디버그

### 환경 이슈 — Phase 2에서 확인된 것들 (처음부터 반영)

```
FM-I1: pl.camera_position = [pos, focal, up]  # camera.view_up setter 없음
FM-I2: imageio.v2.get_writer()                # imageio.v3에는 get_writer 없음
FM-I3: z-buffer 전체 NaN → per-vertex 유클리드 거리를 color channel에 인코딩
```

### `models/vca_processor.py` — attn_processor 어댑터

**hook 방식 금지. 반드시 unet.set_attn_processor() 공식 API 사용.**

```python
import torch
from typing import Optional
from models.vca_attention import VCALayer


class VCAAttnProcessor2_0:
    """
    VCALayer을 diffusers attn_processor 인터페이스로 감싸는 어댑터.

    사용법:
        vca = VCALayer(query_dim=320, context_dim=768, ...)
        proc = VCAAttnProcessor2_0(vca)
        processors = {
            k: proc for k in unet.attn_processors if 'attn2' in k  # cross-attn only
        }
        unet.set_attn_processor(processors)

    주의: encoder_hidden_states는 (BF, N, CD) 형태여야 함.
          표준 CLIP 77토큰 아님 — N entity 임베딩을 별도로 구성해서 넘겨라.
    """
    def __init__(self, vca_layer: VCALayer):
        self.vca_layer = vca_layer

    def __call__(
        self,
        attn,
        hidden_states: torch.Tensor,               # (BF, S, D)
        encoder_hidden_states: Optional[torch.Tensor] = None,  # (BF, N, CD)
        attention_mask: Optional[torch.Tensor] = None,
        **kwargs,
    ) -> torch.Tensor:
        if encoder_hidden_states is None:
            # self-attention fallback: scaled dot-product
            import torch.nn.functional as F
            from einops import rearrange
            BF, S, D = hidden_states.shape
            h = attn.heads
            scale = (D // h) ** -0.5
            q = rearrange(attn.to_q(hidden_states), 'b s (h d) -> b h s d', h=h)
            k = rearrange(attn.to_k(hidden_states), 'b s (h d) -> b h s d', h=h)
            v = rearrange(attn.to_v(hidden_states), 'b s (h d) -> b h s d', h=h)
            out = F.scaled_dot_product_attention(q, k, v, scale=scale)
            out = rearrange(out, 'b h s d -> b s (h d)')
            return attn.to_out[0](out)

        # cross-attention: VCALayer 사용
        return self.vca_layer(hidden_states, encoder_hidden_states)
```

### `scripts/debug_gif.py` — 3-panel GIF 생성

**패널 구조: [생성 프레임 256px | E0 sigma 256px | E1 sigma 256px] → width=768 강제**

```python
"""
make_debug_gif(frames_rgb, sigma_maps, out_path, panel_size=256)

frames_rgb  : list[np.ndarray]  shape (H, W, 3) uint8  — 생성/렌더 프레임
sigma_maps  : list[np.ndarray]  shape (N, H, W) float32 — 프레임별 entity sigma
out_path    : str | Path        — .gif 저장 경로
panel_size  : int               — 각 패널의 px 크기 (기본 256)
"""
```

- sigma heatmap: `matplotlib.cm.hot` (뜨거울수록 높은 밀도)
- 각 패널 우상단에 `"E0 T_mean=0.xx"` 텍스트 오버레이 (PIL.ImageDraw)
- `imageio.v2.mimsave()` 사용 — v3 금지

### `scripts/inject_vca.py` — UNet 주입 유틸리티

```python
def inject_vca(unet, query_dim, context_dim, n_entities=2, z_bins=2, lora_rank=8):
    """
    unet의 모든 cross-attention processor를 VCAAttnProcessor2_0으로 교체.
    반환: {layer_name: VCALayer} dict (sigma 수집용)
    """
```

### 테스트 (`harness/test_phase5_inference.py`)

```python
pytestmark = pytest.mark.phase5

# ─── Processor 단위 테스트 ───
@pytest.fixture(scope="module")
def proc_output():
    from models.vca_processor import VCAAttnProcessor2_0
    from models.vca_attention import VCALayer
    layer = VCALayer(query_dim=64, context_dim=128, n_heads=4, n_entities=2, z_bins=2)
    proc  = VCAAttnProcessor2_0(layer)
    BF, S, D, N, CD = 2, 16, 64, 2, 128
    out = proc(None, torch.randn(BF,S,D), encoder_hidden_states=torch.randn(BF,N,CD))
    return out, layer

def test_processor_output_shape(proc_output):
    out, _ = proc_output
    assert out.shape == (2, 16, 64)

def test_sigma_captured(proc_output):
    _, layer = proc_output
    assert layer.last_sigma is not None
    assert layer.last_sigma.shape == (2, 16, 2, 2)   # (BF,S,N,Z)

def test_transmittance_T0_is_one(proc_output):
    _, layer = proc_output
    T = layer.last_transmittance
    assert torch.allclose(T[:,:,0], torch.ones_like(T[:,:,0]))

# ─── GIF 단위 테스트 ───
@pytest.fixture(scope="module")
def debug_gif_path(proc_output, tmp_path_factory):
    from scripts.debug_gif import make_debug_gif
    _, layer = proc_output
    sigma = layer.last_sigma.numpy()    # (BF,S,N,Z)
    # S=16 → 4×4 spatial (임의 reshape)
    sigma_hw = sigma[0, :, :, 0].reshape(2, 4, 4)  # (N,H,W)
    rgb = (np.random.rand(4,4,3)*255).astype(np.uint8)
    out = tmp_path_factory.mktemp("gif") / "test.gif"
    make_debug_gif([rgb]*3, [sigma_hw]*3, out, panel_size=4)
    return out

def test_debug_gif_created(debug_gif_path):
    assert debug_gif_path.exists() and debug_gif_path.stat().st_size > 0

def test_debug_gif_has_3_panels(debug_gif_path):
    import imageio.v3 as iio
    frame = iio.imread(str(debug_gif_path), index=0)
    assert frame.shape[1] == 4 * 3, f"Expected width={4*3}, got {frame.shape[1]}"
```

---

## Phase 6 — 학습 루프 (PoC)

### 손실 함수 (`models/losses.py`)

세 개의 손실. 각각이 독립적으로 테스트 가능해야 한다.

```python
def l_ortho(depth_pe: torch.Tensor) -> torch.Tensor:
    """depth_pe (Z, CD): z_bins 벡터들이 직교하도록. FM-T4 방지."""
    pe_norm = F.normalize(depth_pe, dim=-1)          # (Z, CD)
    gram = pe_norm @ pe_norm.T                        # (Z, Z)
    eye  = torch.eye(gram.shape[0], device=gram.device)
    return (gram - eye).pow(2).mean()

def l_depth_ranking(
    sigma: torch.Tensor,       # (BF, S, N, Z)
    depth_order: list[int],    # [front_entity_idx, back_entity_idx]
    margin: float = 0.05,
) -> torch.Tensor:
    """
    앞 entity(z=0)의 sigma가 뒤 entity보다 margin 이상 커야 함.
    depth_order = [0, 1] → entity 0이 앞, entity 1이 뒤.
    hinge loss: max(0, sigma_back[z=0] - sigma_front[z=0] + margin)
    """
    front, back = depth_order
    s_front = sigma[:, :, front, 0]   # (BF, S)
    s_back  = sigma[:, :, back,  0]   # (BF, S)
    return F.relu(s_back - s_front + margin).mean()

def l_diff(pred: torch.Tensor, target: torch.Tensor) -> torch.Tensor:
    """기본 diffusion 노이즈 복원 손실 (MSE)."""
    return F.mse_loss(pred, target)
```

### 학습 루프 (`scripts/train_vca.py`)

**UNet 없이 VCALayer 단독 PoC 구조. toy/data에서 depth + mask 로드.**

```python
class ToyVCADataset(Dataset):
    """
    Phase 2에서 생성한 toy 데이터로 VCALayer를 학습.
    
    입력:
      x   = (1, S, D)   프레임 RGB를 PxP 패치로 나눠 D 차원으로 패딩
      ctx = (1, N, CD)  각 entity의 mask 영역 mean color를 CD 차원으로 패딩
    
    감독:
      depth_order = [front_entity_idx, back_entity_idx]
      — entity별 depth map 평균으로 결정
    """

class VCATrainer:
    def __init__(self, vca_layer, dataset, lr=1e-3,
                 w_depth=1.0, w_ortho=0.1):
        # LoRA + depth_pe만 학습
        params = [p for p in vca_layer.parameters() if p.requires_grad]
        self.opt = torch.optim.Adam(params, lr=lr)

    def train_epoch(self, epoch_idx) -> dict:
        # 반환: {'loss': float, 'l_depth': float, 'l_ortho': float}
        ...
        # 매 epoch 종료 후 sigma GIF 저장
        self._save_sigma_gif(epoch_idx, sigma_list)

    def _save_sigma_gif(self, epoch_idx, sigma_list):
        # scripts/debug_gif.make_debug_gif() 호출
        # 저장: {out_dir}/sigma_epoch{epoch_idx:03d}.gif
```

**CLI**:
```bash
python scripts/train_vca.py --epochs 5 --scenario chain --out-dir debug/train
```

### 테스트 (`harness/test_phase6_train.py`)

```python
pytestmark = pytest.mark.phase6

@pytest.fixture(scope="module")
def train_result():
    r = subprocess.run(
        [sys.executable, 'scripts/train_vca.py',
         '--epochs', '2', '--scenario', 'chain', '--batch-size', '2',
         '--out-dir', 'debug/train_test'],
        capture_output=True, text=True, timeout=120
    )
    return r

def test_train_exits_cleanly(train_result):
    assert train_result.returncode == 0, train_result.stderr[-500:]

def test_sigma_gifs_saved(train_result):
    gifs = sorted(Path('debug/train_test').glob('sigma_epoch*.gif'))
    assert len(gifs) >= 2, f"Expected 2+ GIFs, got {len(gifs)}"

def test_loss_finite(train_result):
    import re
    losses = re.findall(r'loss=([\d.eE+\-]+)', train_result.stdout)
    assert len(losses) >= 2
    assert all(math.isfinite(float(l)) for l in losses)

def test_depth_loss_logged(train_result):
    assert 'l_depth' in train_result.stdout

# 손실 함수 단위 테스트
def test_l_ortho_zero_for_orthogonal():
    from models.losses import l_ortho
    # 2D orthonormal: [[1,0,...], [0,1,...]]
    pe = torch.zeros(2, 64); pe[0,0]=1.0; pe[1,1]=1.0
    assert l_ortho(pe).item() < 1e-5

def test_l_depth_ranking_penalizes_wrong_order():
    from models.losses import l_depth_ranking
    sigma = torch.zeros(1, 4, 2, 2)
    sigma[:, :, 0, 0] = 0.2   # entity 0 (front): low sigma
    sigma[:, :, 1, 0] = 0.8   # entity 1 (back):  high sigma — WRONG order
    loss = l_depth_ranking(sigma, [0, 1], margin=0.05)
    assert loss.item() > 0.0  # 패널티 발생해야 함

def test_l_depth_ranking_zero_for_correct_order():
    from models.losses import l_depth_ranking
    sigma = torch.zeros(1, 4, 2, 2)
    sigma[:, :, 0, 0] = 0.9   # entity 0 (front): high sigma
    sigma[:, :, 1, 0] = 0.1   # entity 1 (back):  low sigma — CORRECT
    loss = l_depth_ranking(sigma, [0, 1], margin=0.05)
    assert loss.item() == 0.0  # 패널티 없어야 함
```

---

## Phase 7 — Softmax vs Sigmoid Disappearance Ablation

### 목적

Sigmoid(독립 밀도)와 Softmax(zero-sum)의 Disappearance 현상을 정량 비교.
핵심 논문 주장: "Softmax는 entity를 서로 억제하여 Disappearance를 일으킨다."

### 스크립트 (`scripts/run_ablation.py`)

```python
DISAPPEARANCE_THRESHOLD = 0.15  # entity σ 이하 → "사라진 것"으로 간주

def batch_metrics(sigma):  # (BF, S, N, Z) → dict
    # visibility_min    = min(mean_σ_e0, mean_σ_e1)
    # disappearance_rate = fraction where min σ < threshold
    # both_high         = fraction of spatial positions with σ_e0 > 0.5 AND σ_e1 > 0.5

def train_and_eval(use_softmax, dataset, epochs):
    # 매 epoch 출력: "[sigmoid] epoch=0 loss=X visibility_min=Y disappearance_rate=Z both_high=W"
    # 최종 출력: "FINAL sigmoid visibility_min=X disappearance_rate=Y both_high=Z"
    # 체크포인트: debug/ablation/sigmoid_final.pt, softmax_final.pt
    # 비교 GIF: debug/ablation/comparison.gif (4-panel: Sig-E0 | Sig-E1 | Sof-E0 | Sof-E1)
```

### 테스트 (`harness/test_phase7_ablation.py`)

```python
pytestmark = pytest.mark.phase7

def test_softmax_sigma_sums_to_one():
    # Softmax: sum over N*Z per spatial position ≤ 1

def test_sigmoid_sum_can_exceed_one():
    # Sigmoid: ctx=[+5,+5] → sum > 1 (simultaneous high)

def test_both_high_sigmoid_not_softmax():
    # FM-I4 교훈: ctx=[+5,+5] → both_high_sigmoid > 0.1, both_high_softmax ≈ 0

def test_softmax_disappearance_pattern():
    # ctx=[+10,-10] → Softmax: dominant entity takes all

@pytest.fixture(scope='module')
def ablation_result():
    return subprocess.run(['python', 'scripts/run_ablation.py', '--epochs', '3', ...])

def test_sigmoid_has_lower_disappearance(ablation_result):
    # FINAL sigmoid disappearance_rate ≤ FINAL softmax disappearance_rate

def test_comparison_gif_is_4panel(ablation_result):
    # frame width = 64 * 4 = 256
```

---

## Phase 8 — Sigma-Mask IoU 평가

### 목적

Phase 7에서 저장한 모델의 sigma map이 실제 entity 위치(GT mask)와 얼마나 일치하는지 정량화.
Sigmoid > Softmax인 경우 논문 실험 결과로 사용 가능.

### 스크립트 (`scripts/eval_iou.py`)

```python
def compute_iou(pred, gt):
    # binary mask IoU: inter / max(union, 1)

def sigma_to_binary(sigma_hw, gt_h, gt_w, threshold):
    # (hw, hw) float → PIL resize → binary bool (H, W)

def evaluate_iou(vca, dataset, threshold=0.3):
    # sigma z=0 slice → upsample to GT size → binary → IoU vs GT mask
    # 출력: {'iou_entity0': float, 'iou_entity1': float}

# main: 출력 형식
# sigmoid  iou_entity0=X
# softmax  iou_entity0=Y
# BETTER_MODEL=sigmoid  (or softmax)
```

### 테스트 (`harness/test_phase8_iou.py`)

```python
pytestmark = pytest.mark.phase8

def test_iou_perfect_match():       # pred==gt → IoU=1.0
def test_iou_no_overlap():          # pred∩gt=∅ → IoU=0.0
def test_iou_partial():             # inter=1, union=3 → IoU=1/3
def test_iou_empty_gt():            # union=0 → IoU=0 (not NaN)
def test_sigma_to_binary_resize():  # (8,8) → (16,16)
def test_sigmoid_iou_entity0_positive(iou_result):  # IoU ≥ 0
def test_iou_eval_prints_better_model(iou_result):  # "BETTER_MODEL=" in output
```

---

## Phase 9 — L_ortho Ablation (z-collapse 방지)

### 목적

L_ortho 없이 학습하면 depth_pe가 모든 z에서 동일 방향으로 수렴 → z=0에 모든 attention 집중(z-collapse).
L_ortho가 이를 방지함을 z0_dominance 지표로 입증.

### 지표

```
z0_dominance = sigma[:,:,:,0].mean() / sigma.mean()
  L_ortho 없음: z0_dominance > 0.6  (z-collapse 발생)
  L_ortho 있음: z0_dominance ≈ 0.5  (z 균등 분산)
```

### 스크립트 (`scripts/run_ortho_ablation.py`)

```python
ORTHO_WEIGHT = 0.5   # 강한 L_ortho로 효과 명확히 시연

def z0_dominance(sigma): ...

def train_and_eval(use_ortho, dataset, epochs):
    # 매 epoch: "[no_ortho] epoch=0 loss=X z0_dominance=Y"
    # 최종: "FINAL no_ortho z0_dominance=X"
    # 체크포인트: debug/ortho_ablation/no_ortho_final.pt, with_ortho_final.pt

# main 최종 요약:
# no_ortho   z0_dominance=X
# with_ortho z0_dominance=Y
# RESULT: L_ortho reduces z0_dominance (z-collapse prevented)
```

### 테스트 (`harness/test_phase9_ortho.py`)

```python
pytestmark = pytest.mark.phase9

def test_z0_dominance_uniform():          # 균등 sigma → z0_dominance = 1/Z
def test_z0_dominance_collapsed():        # z=0만 활성 → z0_dominance ≈ 1.0
def test_l_ortho_reduces_gram_off_diagonal():
    # parallel pe → 10 SGD steps → loss 감소

def test_depth_pe_becomes_orthogonal_with_l_ortho():
    # 50 Adam steps → off-diagonal gram < 0.3

def test_ortho_reduces_z0_dominance(ortho_result):
    # FINAL no_ortho z0_dominance ≥ FINAL with_ortho z0_dominance
```

---

## Phase 10 — 학습 유효성 검증 (Encoding Fix + sigma_consistency)

### 배경 — 발견된 문제 3가지 (Phase 10 착수 이유)

**문제 1: x가 의미 없는 dummy**
- 기존: `x[:, :3] = rgb_patches` → 3/64 차원만 채움 → x.std() ≈ 0.006
- Q = x @ W_Q 에서 Q ≈ 0 → sigmoid(0) = 0.5 고착
- **수정**: tile + normalize → x.std() = 0.30

**문제 2: ctx가 거의 동일**
- 기존: `ctx[ei, :3] = color, ctx[ei, 3] = depth` → 나머지 124차원 = 0
- 두 entity ctx 차이 평균 0.001 → K_e0 ≈ K_e1 → entity 분리 불가
- **수정**: entity one-hot(dims 4-5) + entity-specific sin/cos depth encoding → ctx_diff = 0.28

**문제 3: 학습 유효성 검증 없음**
- "loss finite" 만으로는 sigma가 depth 순서를 실제로 배우는지 확인 불가
- **수정**: sigma_consistency (front entity sigma > back entity sigma 비율) 측정

### 10-A: 인코딩 수정 (train_vca.py ToyVCADataset)

```python
# x: tile RGB → normalize to std=0.3
reps = (query_dim + 2) // 3
x = np.tile(x_raw, (1, reps))[:, :query_dim].astype(np.float32)
x = (x - x.mean()) / max(x.std(), 1e-6) * 0.3

# ctx: one-hot identity + entity-specific sinusoidal
ctx[ei, :3]  = mean_color
ctx[ei, 3]   = mean_depth / 10.0
ctx[ei, 4:4+N] = 0.0
ctx[ei, 4+ei]  = 1.0                              # one-hot identity
for k in range(4+N, context_dim):
    freq = (k - 4 - N + 1) * 0.5
    ctx[ei, k] = (0.3 * sin(depth*freq) if ei==0  # e0: sin
                  else 0.3 * cos(depth*freq))      # e1: cos → structurally different
```

**결과**: x.std()=0.300, ctx_diff=0.279

### 10-B: 학습 sanity check (`scripts/check_learning.py`)

```
출력 형식:
ENCODING x_std=0.300 ctx_diff=0.279
BEFORE sigma_consistency=0.5000 sigma_separation=0.0003
AFTER  sigma_consistency=0.8750 sigma_separation=0.0090
LOSS_CURVE 0.0499 0.0487 ... 0.0465
LEARNING=OK
```

- **sigma_consistency**: 각 프레임에서 front entity sigma_z0 > back entity sigma_z0 비율
  - 0.5 = random, > 0.60 = LEARNING=OK
- **sigma_separation**: |mean(sigma_e0) - mean(sigma_e1)| 데이터셋 평균
- **LEARNING=OK 기준**: sigma_consistency > 0.60

**실험 결과** (epoch=10, lr=1e-3, chain scenario):
- BEFORE sigma_consistency=0.50 → AFTER sigma_consistency=0.875
- 인코딩 수정 후 16프레임 중 14프레임에서 올바른 depth 순서 학습 ✓

### 10-C: 테스트 (`harness/test_phase10_learning.py`)

```python
pytestmark = pytest.mark.phase10

def test_x_std_above_threshold():         # x.std() > 0.1 across all frames
def test_ctx_diff_above_threshold():      # ctx_diff > 0.1 across all frames
def test_entity_ctx_structurally_different():  # one-hot dims 4-5 correct
def test_x_not_dummy():                   # non-zero ratio > 50%

@pytest.fixture(scope='module')
def check_result():
    return subprocess.run(['python', 'scripts/check_learning.py', '--epochs', '10', ...])

def test_x_std_printed_and_valid(check_result):       # x_std > 0.1
def test_ctx_diff_printed_and_valid(check_result):    # ctx_diff > 0.1
def test_after_sigma_consistency_above_random(check_result):  # > 0.55
def test_sigma_separation_increases(check_result):    # AFTER > BEFORE
def test_loss_curve_decreases(check_result):          # loss[-1] < loss[0]
def test_learning_ok(check_result):                   # LEARNING=OK
```

### 실패 시 대응

sigma_consistency ≤ 0.60이면:
1. `docs/failures.md`에 FM-I5로 기록
2. toy data의 depth 분포 확인 (frame 0,15는 depth diff ≈ 0 → 이론 한계 ≈ 0.84)
3. epoch 수 또는 lr 조정

---

## Phase 11 — AnimateDiff + VCA 주입 (Real Video Generation)

### 목적
toy에서 검증된 학습(sigma_consistency=0.875)을 실제 AnimateDiff 파이프라인에서 시각적으로 확인.

### 파이프라인 구성
```
base model:      emilianJR/epiCRealism  (SD 1.5, context_dim=768)
motion adapter:  guoyww/animatediff-motion-adapter-v1-5-3  (캐시됨)
scheduler:       DDIMScheduler
dtype:           float16 (FM-A2)
```

### VCA 주입 구조

**FixedContextVCAProcessor** (FM-A1: hook 금지):
```python
class FixedContextVCAProcessor:
    def __call__(self, attn, hidden_states, encoder_hidden_states=None, ...):
        self.call_count += 1
        x = hidden_states.float()           # fp16 → fp32 (내부 계산 안정)
        ctx = entity_context.expand(BF,-1,-1).float()
        vca_out = self.vca(x, ctx)          # returns x + attn_out
        attn_out = vca_out - x              # residual 제거 (transformer block이 처리)
        return attn_out.to(hidden_states.dtype)  # fp32 → fp16
```

**주입 위치**: `mid_block.attentions.0.transformer_blocks.0.attn2.processor`
- query_dim=1280 (SD 1.5 mid_block), context_dim=768 (CLIP)
- VCA 가중치는 fp32 유지 (입출력만 fp16 변환, FM-A2)

**entity context**: CLIP CLS 토큰 (position 0) → (1, N, 768)
- entity0 text → tokenize → text_encoder → last_hidden_state[:, :1, :] = (1, 1, 768)
- concat → (1, 2, 768)

### 아키텍처 수치 (SD 1.5 AnimateDiff)
```
mid_block spatial: 256×256 → VAE(÷8) → 32×32 → UNet(÷2÷2÷2) → 4×4 = 16 tokens
last_sigma shape:  (BF_total=32, S=16, N=2, Z=2)
BF_total = 2 (CFG) × 16 (frames) = 32
conditional slice: sigma[-16:]
```

### 주의: 미훈련 VCA에서 sigma_separation ≈ 0
```
random init VCALayer → Q, K 모두 랜덤 → sigmoid(Q@K.T) ≈ 0.5 (중심 대칭)
entity0_mean ≈ entity1_mean ≈ 0.5 → sigma_separation ≈ 0 (round 시 정확히 0.0)

실험 결과:
  entity0_mean_sigma = 0.5302
  entity1_mean_sigma = 0.5302
  sigma_separation   ≈ 0.0 (미훈련)
  both_high_ratio    = 0.87  ← Sigmoid 특성 확인됨 ✓

→ 미훈련 상태는 정상. 훈련 후 separation 증가 (toy: sigma_consistency=0.875).
→ 테스트는 sigma_separation >= 0 + both_high_ratio > 0 으로 검증.
```

### 스크립트 (`scripts/run_animatediff.py`)

```bash
# 단일 프롬프트
python scripts/run_animatediff.py \
    --prompt "Two robotic arms crossing" \
    --entity0 "a red robotic arm" \
    --entity1 "a blue robotic arm" \
    --out-dir debug/animatediff/robot_arm \
    --steps 20 --num-frames 16 --height 256 --width 256 --seed 42

# 전체 3개 프롬프트
python scripts/run_animatediff.py --run-all
```

### 출력물
```
debug/animatediff/{dir}/
    baseline.gif    # VCA 없이 표준 AnimateDiff 생성
    generated.gif   # VCA 주입 후 생성
    debug.gif       # [RGB | E0 sigma | E1 sigma] 3-panel, panel_size=256
    sigma_stats.json
```

### 테스트 (`harness/test_phase11_animatediff.py`)

```python
pytestmark = [pytest.mark.phase11, pytest.mark.slow]
# 기본 실행 제외: pytest.ini addopts = -m "not slow"
# 명시 실행: pytest -m phase11

def test_fixed_context_vca_processor_interface()  # call_count, dtype, shape
def test_vca_fp16_input_handled()                 # FM-A2: fp16 → fp32 → fp16
def test_pipeline_loads()                         # AnimateDiffPipeline 로드
def test_vca_injected()                           # mid_block attn2에만 주입됨

@pytest.fixture(scope='module')
def run_result():  # subprocess run_all, timeout=1800s

def test_all_gifs_created(run_result)         # 9개 파일 (3 dirs × 3 gifs)
def test_sigma_stats_exist_and_valid(run_result)
def test_sigma_separation_nonnegative(run_result)  # abs() ≥ 0
def test_both_high_ratio_positive(run_result)      # Sigmoid: > 0
def test_debug_gif_is_3panel(run_result)           # width = 256 × 3
def test_baseline_differs_from_generated(run_result)  # pixel diff > 0.5
def test_vca_processor_was_called(run_result)      # FM-A1: call_count > 0
def test_generated_gif_frame_count(run_result)     # 16 frames
```

### 실행 방법
```bash
# Phase 11만 실행 (모델 다운로드 포함, 수십 분 소요)
pytest -m phase11 -v

# Phase 0-10만 (기본, 빠름)
pytest  # slow 자동 제외
```

---

## 절대 원칙

- **Sigmoid는 Softmax가 아니다.** `test_entities_independent`가 이를 강제한다. (Phase 1의 마지막 테스트)
- **`--lf` 써라.** 전체를 다시 돌리지 말고 실패한 테스트만 재실행해라.
- **Phase skip 금지.** conftest가 자동으로 막는다. 억지로 우회하지 마라.
- **실패를 기록해라.** Phase 4 테스트가 FM-I 섹션을 강제로 요구한다.
- **hook 방식 금지.** Phase 5부터는 반드시 `unet.set_attn_processor()` 사용.
- **imageio.v2.get_writer.** GIF/MP4 쓰기는 v2, 읽기는 v3.
- **z-buffer 믿지 마라.** headless에서 NaN → per-vertex 유클리드 거리 인코딩 사용.
- **`pv.camera_position = [pos, focal, up]`** — camera.view_up setter 없음.

---

## Phase 12 — AnimateDiff VCA 실제 파인튜닝

**목표**: Phase 11 파이프라인 위에서 VCALayer(LoRA + depth_pe)만 학습.
UNet은 frozen, toy/data 영상의 실제 mid_block hidden_states로 감독.

### Phase 10과의 차이

| | Phase 10 (toy PoC) | Phase 12 (실제 학습) |
|---|---|---|
| query_dim | 64 | 1280 (mid_block 실제 차원) |
| context_dim | 128 | 768 (CLIP 임베딩) |
| x 소스 | RGB 패치 dummy | UNet mid_block hidden_states |
| L_diff | 없음 | 있음 (표준 diffusion MSE) |
| UNet | 없음 | frozen (requires_grad=False) |

### 핵심 구현 원칙

- **FM-A1**: hook 금지 → `unet.set_attn_processor()` (Phase 11과 동일)
- **FM-A2**: UNet fp16, VCALayer fp32. 입출력만 dtype 변환
- **FM-I6**: loss 계산은 반드시 `last_sigma_raw` (grad 있음). `last_sigma`(detach)로는 gradient=0
- **UNet frozen 검증**: `assert sum(p.requires_grad for p in unet.parameters()) == 0`
- **gradient checkpointing**: `pipe.unet.enable_gradient_checkpointing()` (메모리 절약)

### 손실 구조

```
L_total = L_diff + 0.1 * L_depth_ranking + 0.05 * L_ortho

L_diff: F.mse_loss(pred_noise, noise)  -- autocast(fp16) 밖에서 fp32 계산
L_depth_ranking: hinge loss, sigma_raw[:,:,front,0] > sigma_raw[:,:,back,0]
L_ortho: ||G - I||_F²  (depth_pe 직교성, FM-T4 방지)
```

### TrainVCAProcessor (학습용)

```python
class TrainVCAProcessor:
    def __call__(self, attn, hidden_states, ...):
        x = hidden_states.float()        # fp16 → fp32
        vca_out = self.vca(x, ctx)       # sets last_sigma_raw (with grad)
        attn_out = vca_out - x           # 이중 잔차 방지
        return attn_out.to(hidden_states.dtype)
```

### 출력물 (--scenario chain --epochs 5 --n-frames 4 기준)

```
checkpoints/animatediff_test/
  chain_epoch004.pt    # {vca_state_dict, epoch, loss, scenario}
  chain_best.pt
debug/train_animatediff_test/
  chain_sigma_epoch004.gif   # [RGB | E0σ | E1σ] 3-panel
  final_generated.gif        # 학습된 VCA로 생성
  final_baseline.gif         # VCA 없는 baseline
  training_curve.json        # epoch별 loss, sigma_separation
```

### stdout 파싱 형식

```
epoch=  0  loss=0.4321  l_diff=0.4201  l_depth=0.0089  l_ortho=0.0031  sigma_sep=0.001
...
FINAL sigma_separation=0.XXX
FINAL sigma_consistency=0.XXX
LEARNING=OK  # or LEARNING=FAIL
```

### LEARNING=OK 조건

1. `sigma_separation (AFTER) > sigma_separation (BEFORE = 0.0)`
2. `sigma_consistency > 0.55`
3. `l_depth 후반 < l_depth 초반` (감소 경향)

### 테스트 (`harness/test_phase12_train_animatediff.py`)

```python
pytestmark = [pytest.mark.phase12, pytest.mark.slow]

# 유닛 테스트 (subprocess 없음)
def test_train_vca_processor_interface()     # last_sigma_raw grad 있음 (FM-I6)
def test_train_vca_processor_fp16_input()    # FM-A2: fp16→fp32→fp16
def test_unet_frozen_after_inject()          # requires_grad=False 검증
def test_encode_frames_shape()               # (1,4,T,H//8,W//8)
def test_sigma_stats_train()                 # separation ≥ 0, consistency 반환
def test_depth_order_parsing()               # front/back {0,1}

# subprocess 통합 테스트
@pytest.fixture(scope='module')
def train_result():  # 5 에폭, 4프레임, timeout=600s

def test_train_exits_cleanly(train_result)
def test_checkpoint_saved(train_result)         # chain_best.pt 존재
def test_checkpoint_loadable(train_result)      # vca_state_dict 로드 가능
def test_sigma_separation_increases(train_result) # FINAL > 0.0
def test_learning_ok(train_result)              # LEARNING=OK
def test_sigma_gif_created(train_result)        # chain_sigma_epoch*.gif
def test_loss_components_logged(train_result)   # l_diff, l_depth, l_ortho
def test_loss_not_exploding(train_result)       # loss < 10.0
def test_final_generated_gif_created(train_result)
def test_final_baseline_gif_created(train_result)
def test_final_differs_from_baseline(train_result)  # pixel diff > 0.5
def test_training_curve_saved(train_result)     # training_curve.json
```

### 실행 방법

```bash
# Phase 12 전체 실행 (GPU 필요, ~10분)
pytest -m phase12 -v

# 20에폭 full run
python scripts/train_animatediff_vca.py --scenario chain --epochs 20

# 두 시나리오 모두
python scripts/train_animatediff_vca.py --scenario all --epochs 20
```
